Office.onReady(() => {
  // The ribbon button uses a ShowTaskpane action defined directly in the
  // manifest, so no custom function is required here. This file exists
  // as the function-file target Outlook expects for command surfaces.
});
